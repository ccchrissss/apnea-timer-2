import styled from 'styled-components'

export const StyledInputsAndPresets = styled.section`

  margin: 1rem;
  display: flex;

  .inputs {
      display: flex;
      flex-basis: 50%;
      flex-wrap: wrap;
      row-gap: 10px;
      column-gap: 5px;
  }

  .preset-btns-big-box {
      display: flex;
  }

  .preset-btns-sml-box {
      display: flex;
      column-gap: 5px;
  }


`